# Redis Configuration

namespace: meogle

image: redis:8-alpine
replicas: 1
port: 6379
storage: 5Gi
storageClass: ""  # Use default storage class

resources:
  requests:
    memory: "128Mi"
    cpu: "50m"
  limits:
    memory: "256Mi"
    cpu: "250m"