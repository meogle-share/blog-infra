apiVersion: v1
kind: Service
metadata:
  name: {{ include "meogle-was.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "meogle-was.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "meogle-was.selectorLabels" . | nindent 4 }}
