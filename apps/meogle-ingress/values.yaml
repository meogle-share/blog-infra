# Meogle Ingress Configuration (Traefik)

namespace: meogle

domain: www.meogle.co.kr

# Backend service references
was:
  serviceName: meogle-was
  port: 3001

ui:
  serviceName: meogle-ui
  port: 3000

ingress:
  enabled: true
  tls:
    enabled: false
    secretName: meogle-tls-secret